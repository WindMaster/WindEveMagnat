<UserControl x:Class="WindEveMagnat.UI.Controls.EveItemsTree"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200" MinHeight="300" MinWidth="200" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
    <Grid>
		<TreeView Name="treeViewItems" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" MinHeight="300" Height="200" MinWidth="200" MouseDoubleClick="treeViewItems_MouseDoubleClick">
			<TreeView.ContextMenu>
				<ContextMenu>
					<MenuItem Header="Add all" Click="MenuItemClick">
					</MenuItem>
				</ContextMenu>
			</TreeView.ContextMenu>
		</TreeView>
	</Grid>
</UserControl>
